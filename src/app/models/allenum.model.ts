export enum Gender {
    MALE, FEMALE, OTHER
}

export enum ValidateStatus {
    NONE, BAD, GOOD, BEST, PERFECT, AVRAGE, VALID, INVALID
}

export enum VisiblityStatus{
	FIRST,SECOND,THIRD,FOURTH
}

export enum Role{
    ADMIN,VALIDATOR,USER
}

export enum SalaryStatus{
    PAID=1,
    UNPAID=0
}